# Go Conference 2026 プロポーザル

- 応募先: https://sessionize.com/go-conference-2026/ (締切 2026-06-15 JST)
- 枠: **ショートワークショップ(40分)** / 中級者向け / 日本語
- 参加者上限: 30名

---

## タイトル

**40分で40倍速!ルーフラインで読み解くPure Go × SIMDベクトル検索**

## エレベーターピッチ

Go 1.26の実験的SIMDパッケージ(`simd/archsimd`)で、外部ライブラリなしのPure Goベクトル検索を高速化します。ただし闇雲には触りません。**ルーフラインモデル**——「測る → 算術強度を出す → 当たっている天井を特定 → その天井を狙う手だけ打つ」——という一本の地図に沿って、SIMD化と量子化の実コードを手元で動かし、ベンチマークで数十倍を体感する40分。環境はCodespaces配布、必要なのはGitHubアカウントとブラウザだけです。

## 概要(Description)

Go 1.26で標準ライブラリに実験的SIMDパッケージ(`simd/archsimd`)が入りました。アセンブリもcgoも書かずに、Pure GoでCPUのベクトル命令を直接叩ける時代の始まりです。

このワークショップの題材は、SIMDが最も「効く」応用のひとつであるベクトル検索エンジン——RAGやセマンティック検索を支える中核技術——を、外部ライブラリなしのPure Goで作ること。ですが本当のテーマは**高速化の「進め方」**です。「とりあえずSIMD化して、効かなかったら次の手を試す」のではなく、**ルーフラインモデル**という一枚の図の上で、いま自分のコードがどの天井に当たっているかを見て、打つべき手を決める——その回し方を体験します。前提知識はGoの基礎と `go test` が使えることだけで、CPUアーキテクチャの予備知識は不要です。

進め方は毎回同じ4手です。**①測る(GFLOP/sと転送量) → ②算術強度AI(flop/byte)を計算して図に点を打つ → ③当たっている天井を特定する(メモリ斜線か・演算横線か・どちらにも未達か) → ④その天井を狙う手だけ打つ**。このループを、2段階の高速化で実際に回します。

1. **縦に上る(SIMD化)**: naiveな内積ループを `Float32x8` + FMA(`MulAdd`)で書き換えた実コードを読み、手元で動かします。ルーフライン上では、内積カーネルのAIは **0.5 flop/byte**——リッジの遥か左で、図は「これは構造的にメモリ律速だ」と**まだ最適化する前から教えてくれます**。実際にやると、カーネル単体は **4.6倍**(204ns→45ns)なのに検索全体は **1.6倍** 止まり。この落差は、ルーフライン上で「カーネル単体(L1常駐)と検索全体(DRAMストリーム)が別々の天井に当たっている2つの点」として読み解けます。検索全体はメモリ斜線にピッタリ張り付き、**ベクトル幅を広げても天井は動かない**ことを、参加者自身のベンチで確かめます

2. **横に動く(バイナリ量子化)**: メモリ斜線に当たったら、ルーフラインの指示は「AIを右へ動かせ＝バイトを削れ」。ベクトルを1bit化してデータ量を1/32にし、距離計算をXOR+popcountのハミング距離へ。153MBが4.8MBになってキャッシュに乗り、DRAM律速のregimeから抜けます。実測 **43倍**。スカラーのpopcountがSIMD実装を抜き去る瞬間です

SIMDだけが高速化ではありません。ルーフラインは「縦に上る(実装効率)」と「横に動く(データ表現)」が別軸の動きだと一枚で見せてくれる。だから「壁に張り付いた→右へ逃げる」という次の一手を、勘ではなく**図から選べる**。さらに横に動いた先(binary表現)でまたSIMDが効く(AVX-512のVPOPCNT)——この往復を、実行時CPU機能チェック(`archsimd.X86`)の実演も交えて、嘘のないベンチマーク数字で体感してもらいます。

あわせて、「SIMD化したのに速くならない」を切り分ける診断技術も、すべてルーフラインの4手に位置づけて体験できます。カーネル単体と検索全体の2粒度を比べて計算律速かメモリ律速かを見抜く(=点を2つ打つ)、ベンチのスケールを変えて固定費を分離する、`go tool objdump`で生成コードを確認する、Recall@10で精度への影響を定量化する——といった手順を教材に組み込んでいます。

環境は構築済みのGitHub Codespacesを配布するため、**必要なのはGitHubアカウントとブラウザだけ**。手元にAMD64マシンがなくても、全員が同じルーフライン図の上に同じ点を打てます。

## 運営者向けノート(Notes)

- **進行設計**: 実装済みリポジトリを配布。各 Stage の**実コードを読み**、直後に `make roofline` で **GFLOP/s・GB/s・AI を表示して図に点を打つ**構成(穴埋めはせず、コードはそのまま手元で走る)。ルーフライン図はインタラクティブHTML(`docs/workshop/workshop.html`)で全員のスクリーンに出し、点をクリックすると当たっている天井とGoコードが切り替わる。各 Stage の実コード・実行コマンド・出る数字・つまずきどころの解説はこの HTML に集約(読み物として通読でき、当日もこの順で進める)
- **教材の背骨**: `docs/workshop/workshop.html` に「測る→AI→天井→手」のループと、c7i実測ベースの本物のルーフライン(メモリ帯域11GB/s STREAM・演算ピーク39GF実測(AVX2理論~120/AVX-512理論240)・kernel AI 0.5・各Stageの点と天井)を定義済み。天井は `make roofline-ceiling` のマイクロベンチで実測。すべての文章・コードはここから派生
- **環境リスク対策**: Codespaces prebuildを事前案内で起動依頼。AVX-512非搭載VMでも動くようAVX2(256bit)を基準に設計し、AVX-512(VPOPCNT)はボーナス章 + 講師のAVX-512保証VMでのライブデモとします。会場ネットワーク障害時は講師画面でのライブ進行に切替可能
- **実測済み**: 応募時点でStage 0〜2を実装済みで、概要の数字はAVX-512搭載VM(AWS c7i / Sapphire Rapids)上の実測値です。当日環境のCodespacesでも開催前に再計測します(リポジトリ: github.com/po3rin/gocon2026-simd-search)
- **参加者の費用負担なし**: 環境はGitHub Codespacesの個人無料枠(月120コア時間)内で完結します
- **新規性**: `GOEXPERIMENT=simd` は2026年2月リリースの最新機能で、手を動かして学べる日本語教材はまだほぼ存在しません。さらに「ルーフラインで打つ手を決める」という最適化の地図を、SIMDの実装体験と一枚に重ねた教材は希少です
- **テーマとの合致**: 標準ライブラリだけでGoの適用領域を高性能計算へ広げる(Go Far)体験を、30人全員が同じルーフライン図に点を打ちながら進めます(Go Together)

## タイムテーブル(40分)

| 時間 | 内容 | 参加者がやること |
|---|---|---|
| 0–5分 | 導入: `GOEXPERIMENT=simd` とは / ルーフラインの読み方「測る→AI→天井→手」 | 聞く(Codespacesは事前起動済み) |
| 5–9分 | Stage 0: naive全探索を測り、AI=0.5の点を図に打つ → 「どの天井にも未達」を確認 | `make roofline` で点を打つ |
| 9–20分 | Stage 1: 内積SIMD化の実コードを読む → カーネル4.6x / 全探索1.6x の落差を図上の2点として読む | `make roofline` で計測 🎉 |
| 20–31分 | Stage 2: メモリ斜線に当たった→「横に動け」。バイナリ量子化+ハミング距離の実コードを読む | `make roofline` / `recall` で計測 🎉🎉 |
| 31–37分 | Recall@10計測 → rerankで精度回復 → レースデモ | `make recall` / `make race` |
| 37–40分 | まとめ: 縦に上る/横に動くの2軸、AVX-512ボーナス、int8・バッチ化の持ち帰り | — |

## 当日の演出メモ(プロポーザルには載せない)

- 各Stage後に `docs/workshop/workshop.html` の該当点をクリックして全員で「いまここ」を共有する(点が縦に上り、Stage 2で横へ飛ぶ動きを見せる)
- Stage 1完了直後に `go build -gcflags=-S` で `VFMADD231PS` が出ていることを30秒見せる(「あなたのGoコードがこのCPU命令になった」)
- Stage 1の「全探索1.6x止まり」で、図のメモリ斜線に点が張り付くのを見せてから「幅を倍にしても天井は動かない」を予言→確認の順で出す(ルーフラインの予言力の山場)
- 量子化のrecall劣化は「具体的な誤ヒット例」を1つ仕込んでスクリーンに出す(数字より笑いと納得)
- 早く終わった人向けの改造ネタ: アキュムレータ本数を変える、`Float32x16`(AVX-512)に差し替え、`Uint64x4.OnesCount`、int8量子化でAIを右へ
- AVX-512デモ用に AWS c7i / GCP c3 など Sapphire Rapids世代のVMを1台用意

## 実測値(AWS c7i / Xeon 8488C、Go 1.26.4、10万ベクトル×384次元)

| Stage | ルーフライン上の位置 | 実測 | 倍率 |
|---|---|---|---|
| 0: naive 全探索 | AI 0.5・どの天井にも未達 | 27.0 ms/query (2.85 GF / 5.7 GB/s) | 1.0x |
| 1: SIMD 内積(カーネル単体) | L1常駐・演算側で上げ代 | 204 → 44.7 ns | **4.6x** |
| 1: SIMD 全探索 | AI 0.5・メモリ斜線に張り付き(9.2 GB/s ≒ 帯域の84%) | 16.7 ms/query | 1.6x ← 図の予言が当たる山場 |
| 2: バイナリ量子化(byte 1/32) | 横に動いてDRAM律速を脱出 | 0.62 ms/query | **43x** |
| 仕上げ: + float32 rerank | 精度軸(Recall@10 0.18→0.87) | 0.73 ms/query | **39x** |
| bonus: unsafe + VZEROUPPER 最適化 | sub-ceilingを掃除しメモリ天井へ | 23.5 ns(カーネル) | 8.7x |

※ Codespaces(本番環境)での再計測は応募後に実施。
※ 調査の副産物として「Go 1.26 simd は VZEROUPPER を自動挿入せず、SIMD関数の呼び出し
ごとに〜550cycleの隠れ税が発生しうる」という(おそらく)未報告の知見を得た。ルーフライン上では
「メモリ天井に届く前に越えるべき隠れ sub-ceiling」として現れる。詳細は docs/dev/OPTIMIZATION_LOG.md。
golang/go への issue 報告予定。
